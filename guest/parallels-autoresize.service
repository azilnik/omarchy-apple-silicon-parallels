[Unit]
Description=Follow Parallels window size (adaptive HiDPI auto-resolution)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/omarchy-parallels-autoresize
Restart=always
RestartSec=3

[Install]
WantedBy=graphical-session.target
